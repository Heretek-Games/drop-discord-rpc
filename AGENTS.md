# AGENTS.md — drop-discord-rpc

Discord Rich Presence client plugin for Drop (#15).

## Commands

```sh
pnpm install
pnpm build
pnpm test
```

Built on `@droposs/plugin-sdk` (plugin API v2); the dependency resolves the
sibling checkout at `../drop-plugin-sdk/packages/plugin-sdk`.
